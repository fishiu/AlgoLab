#include <iostream>
#include <algorithm>


using PI = std::pair<int, int>;


int main() {
    int a = -1, b = -2;
    std::cout << -3 / 2 << std::endl;
    std::cout << (a + b) / 2 << std::endl;

    return 0;
}